# Sample Python App

This tiny app loads fictional character data, formats short summaries, and
prints a small report. It exists so the code RAG indexer has clear functions,
comments, and file boundaries to retrieve.

Useful search targets:

- `load_demo_data` loads the in-memory records.
- `format_character_summary` formats one character.
- `build_report` assembles the final text report.
