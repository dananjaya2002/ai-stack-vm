# Sample Repository App

This tiny repository exists for the dashboard repository browser and code
indexing demo. It is intentionally small, but it has a realistic layout with
source files, tests, and configuration.

Useful search targets:

- `describe_repository` summarizes repository metadata.
- `load_repository_profile` returns the demo repository profile.
- `tests/test_repository_summary.py` shows expected summary behavior.
