[project]
name = "sample-repository-app"
version = "0.1.0"
description = "Demo repository for AI Stack code indexing and repository browsing."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
